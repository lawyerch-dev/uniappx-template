<template>
  <scroll-view style="flex: 1">
    <view class="home">
      <text class="home__title">{{ t('home.title') }}</text>
      <text class="home__desc">{{ t('home.desc') }}</text>

      <view class="home__card">
        <text class="home__label">{{ t('home.devref') }}</text>
        <button class="home__btn" hover-class="home__btn--press" @click="openExamples()">{{ t('home.btn.examples') }}</button>
      </view>
    </view>
  </scroll-view>
</template>

<script setup lang="uts">
  import { useI18n } from 'vue-i18n'

  const { t } = useI18n()

  const openExamples = (): void => {
    uni.navigateTo({
      url: '/examples/tabBar/component'
    })
  }

  defineExpose({
    openExamples
  })
</script>

<style>
  .home {
    padding: 16px;
    min-height: 100%;
    background-color: var(--page-bg);
  }

  .home__title {
    font-size: 20px;
    font-weight: bold;
    color: var(--text-primary);
  }

  .home__desc {
    margin-top: 8px;
    font-size: 14px;
    color: var(--text-secondary);
  }

  .home__card {
    margin-top: 16px;
    padding: 16px;
    border-radius: 12px;
    background-color: var(--card-bg);
    border-width: 1px;
    border-style: solid;
    border-color: var(--card-border);
  }

  .home__label {
    font-size: 16px;
    color: var(--text-primary);
  }

  .home__btn {
    margin-top: 12px;
    font-size: 14px;
    border-radius: 8px;
    background-color: var(--accent);
    color: #ffffff;
  }

  .home__btn--press {
    opacity: 0.85;
  }
</style>
