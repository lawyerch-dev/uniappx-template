<template>
  <scroll-view style="flex: 1">
    <view class="home">
      <text class="home__title">我的应用</text>
      <text class="home__desc">业务首页。官方示例已归档到 examples/ 目录，可按需查看。</text>

      <view class="home__card">
        <text class="home__label">开发参考</text>
        <button class="home__btn" @click="openExamples()">查看官方示例</button>
      </view>
    </view>
  </scroll-view>
</template>

<script setup lang="uts">
  const openExamples = (): void => {
    uni.navigateTo({
      url: '/examples/tabBar/component'
    })
  }

  defineExpose({
    openExamples
  })
</script>

<style>
  .home {
    padding: 16px;
  }

  .home__title {
    font-size: 20px;
    font-weight: bold;
    color: #333333;
  }

  .home__desc {
    margin-top: 8px;
    font-size: 14px;
    color: #999999;
  }

  .home__card {
    margin-top: 16px;
    padding: 16px;
    border-radius: 8px;
    background-color: #f8f8f8;
  }

  .home__label {
    font-size: 16px;
    color: #333333;
  }

  .home__btn {
    margin-top: 12px;
    font-size: 14px;
    border-radius: 6px;
    background-color: #007aff;
    color: #ffffff;
  }
</style>
